# Security Policy

- 不要把真实 API Key、数据库密码或含敏感数据的 `.env` 提交到仓库。
- 默认 SQLite 和哈希嵌入模式完全在本机运行。
- 使用远程模型或解析服务前，确认文档的数据分类和传输许可。
- Docker Compose 中的 Neo4j 密码仅用于本地演示，部署前必须替换。
- 发现安全问题时，请提供最小复现步骤，不要附带真实财务材料或凭据。
